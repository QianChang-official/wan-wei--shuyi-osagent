# 演示场景

运行 `python scripts/demo_governance.py --api-key <key>`，脚本按五步生成现场证据：

1. 写入含敏感信息的记忆，展示 Policy Gate 标记；
2. 跨会话检索召回；
3. 指定记忆 hard delete；
4. 对主表、FTS、图边、向量引用、遗留表逐项验证为零；
5. 导出含审计编号的 PDF 删除证书。

真实业务叙事覆盖政务办公续接项目、知识治理批量遗忘、长期协作偏好积累。服务须先启动，脚本不会伪造成功结果。
