# 宛委·枢忆 使用说明书

面向最终用户与运维的安装、配置与日常使用指南。开发者文档（架构设计、创新点、评测口径）已随 v1.0 发布归档精简；本手册只回答「怎么装、怎么用、怎么排障」。

---

## 一、系统要求

| 项目 | 要求 |
|---|---|
| 操作系统 | 银河麒麟 V11（x86_64）桌面版；其他 Linux 发行版可源码运行 |
| Python | 3.10+（麒麟 V11 自带 3.12，无需另装） |
| Node.js | 22.12+（仅从源码构建桌面端时需要；安装 RPM/DEB 包不需要） |
| 磁盘 | ≥ 2 GB（含向量库与日志） |
| 内存 | ≥ 4 GB |
| 可选 | 麒麟原生 AI 向量引擎（`kylin-ai-vector-engine`，系统预装）——启用后走原生语义检索 |

---

## 二、安装

### 方式 A：RPM 安装包（推荐，麒麟 V11）

```bash
# 联网环境：安装时会自动拉取 Python 依赖
sudo dnf install ./wanwei-shuyi-desktop-1.0.0.x86_64.rpm
# 或
sudo rpm -ivh wanwei-shuyi-desktop-1.0.0.x86_64.rpm
```

安装内容：
- 桌面应用：`/opt/wanwei-shuyi-desktop/`（Electron 主程序，应用菜单可直接启动）
- 内嵌后端：`/opt/wanwei-shuyi-desktop/resources/backend/`，随桌面应用自动拉起（本机回环 127.0.0.1，不对外暴露）
- systemd 用户服务单元：`/etc/systemd/user/wanwei-shuyi-desktop.service`

### 方式 B：DEB 安装包（Debian 系）

```bash
sudo dpkg -i wanwei-shuyi-desktop_1.0.0_amd64.deb
```

### 方式 C：源码运行（开发者）

```bash
bash scripts/setup.sh        # 创建 venv 并安装依赖
bash scripts/run_dev.sh      # 启动后端 + 控制台
```

控制台入口：**http://127.0.0.1:8010/console/**

---

## 三、首次启动

1. 启动桌面应用（应用菜单搜索「宛委」），或源码方式运行 `run_dev.sh`。
2. 后端首次启动自动完成数据库初始化（SQLite WAL 模式，无需手工建库）。
3. 进入控制台「万枢工作台」即可对话——**必须先接入模型供应商**：未配置网关时后端如实返回配置或供应商错误，不再回退任何模拟通路（`local_mock` 已随 issue #45 删除）。
4. 接入真实模型：进入「模型接入」视图，从 31 家供应商目录中任选一家，填入 API key 保存。OpenAI 兼容云端供应商（含 DeepSeek）与 AWS Bedrock 为真实调用，其余标注 stub。

**桌面应用启动后无响应？** 见第六节 KySec 排障。

---

## 四、日常使用

### 4.1 记忆指令

在控制台任意对话中说「记住……」，系统经 Policy Gate 校验后写入记忆胶囊（拦截密码 / 密钥 / 投毒内容）。可在「记忆中枢」视图查看与编辑。

### 4.2 记忆治理端点（嵌入式集成）

所有端点默认仅监听本机回环；写操作需带 `X-API-Key` 请求头（回环 GET 免密只读）。

| 域 | 端点 | 说明 |
|---|---|---|
| 写入 | `POST /memory/v2/capsules` | 写入记忆胶囊（过 Policy Gate） |
| 检索 | `GET /memory/v2/search?q=...&top_k=5` | 混合检索（原生向量 / FTS 自动选择） |
| 遗忘 | `POST /memory/forget/confirm` | 删除并五处取证 |
| 删除证明 | `GET /memory/governance/verify-deletion/{id}/certificate` | 导出 PDF 删除证书 |
| 账本 | `GET /memory/ledger/{id}` | 单条记忆的不可变操作历史 |
| 健康度 | `GET /memory/health` | 记忆库健康评分 |

### 4.3 一键证据演示

```bash
python scripts/demo_governance.py --api-key <key>
```

脚本按步骤 0–5 现场生成证据：健康检查 → **写入记忆**（先以明文口令演示 Policy Gate S3 硬拦截 `reject`，再写入一条可治理的敏感内部知识）→ 跨会话检索召回 → 指定记忆硬删除 → 五处残留逐项取证 → 导出含审计编号的 PDF 删除证书。

拦截段是**设计性拒绝**：脚本如实呈现门禁结果，若明文口令未被拦下会打印告警提示核对 Policy Gate 规则，不会绕过或伪造成功。

> 需在后端已启动的**前台会话**内运行（先 `bash scripts/run_dev.sh`）。检索端点 `/memory/v2/search` 已由 POST 改为 GET，旧版调用方式会返回 405。麒麟 V11 实机全链路通过记录见 `reports/kylin-vm-evidence-20260908/09c-demo-governance.txt`。

### 4.4 常用脚本

| 脚本 | 用途 |
|---|---|
| `scripts/run_dev.sh` | 启动后端 + 控制台 |
| `scripts/smoke.sh` / `smoke.py` | 冒烟自检 |
| `scripts/demo_governance.py` | 五步证据链演示 |
| `scripts/run_meb.py` | MEB 记忆评测（mini/full） |
| `scripts/bench_latency_scale.py` | 检索延迟规模基准（1k/10k/50k） |
| `scripts/bench_kylin_sdk_latency.py` | 麒麟 SDK 端到端延迟基准（预热剔除 + 逐次原始延迟；非麒麟环境如实退出，不编造数据） |
| `scripts/bench_baseline_compare.py` | 四臂消融对照（hybrid / fts_only / vector_only / no_governance） |
| `scripts/bench_tke.py` | TKE 知识演化基准（as-of 回放正确率） |
| `scripts/bench_egpm.py` | EGPM 偏好事件级消融基准（同时产出 JSON 与 Markdown 报告） |
| `scripts/license_header.py` | 木兰 PSL v2 版权头批量写入工具（幂等） |

---

## 五、配置（环境变量）

| 变量 | 默认 | 说明 |
|---|---|---|
| `WANWEI_API_KEY` | 无 | API key；未设置时写操作仅限本机回环且需 key，生产部署必须设置（≥32 字符） |
| `WANWEI_DATA_DIR` | 无（回退见下） | 数据根目录；未设置时 Windows 取 `%LOCALAPPDATA%\wanwei-shuyi`，Linux 取 `$XDG_DATA_HOME/wanwei-shuyi`，再回退 `~/.local/share/wanwei-shuyi`。桌面应用启动后端时另由 `WANWEI_MEMORY_DB` 显式指向 `~/.config/wanwei-shuyi-desktop/runtime/memory.db` |
| `WANWEI_MEMORY_DB` | `<数据目录>/memory.db` | SQLite 记忆库路径；显式设置时覆盖上一行的推导结果 |
| `WANWEI_KYLIN_NATIVE_MODE` | `auto` | `auto` 优先麒麟原生向量检索；`off` 强制 FTS |
| `WANWEI_KYLIN_SDK_PERSISTENT` | `1` | 常驻原生 SDK bridge（模型只加载一次，检索延迟 ~15ms）；`0` 回退每次 spawn 的旧模式（排障用） |
| `WANWEI_KYLIN_SDK_TIMEOUT_SECONDS` | `10` | 原生 SDK 单请求超时 |
| `WANWEI_KYLIN_VECTOR_ENCRYPT` | 关 | `1` 启用向量库文件加密加载（官方 `LoadDBFile(db, encrypt, key)`）；加密与明文库文件不兼容，开启前需重建或迁移既有 `kylin-vector.db`（删除后由检索写入自然重建，或先备份） |
| `WANWEI_KYLIN_VECTOR_KEY_FILE` | 无 | 加密 key 文件路径（建议权限 `600`）；key 仅从该文件读取，不进入协议、参数或日志。加密开启而文件缺失/不可读时，原生检索明确降级为 FTS（`vector_encryption_key_unavailable`），不带病启动 |
| `WANWEI_CORS_ORIGINS` | 无 | 跨源白名单（逗号分隔）；不接受 `*` |
| `WANWEI_OUTCOME_VALIDATION` | off | 偏好执行结果反馈闭环开关 |

---

## 六、排障

### 6.1 桌面应用 / venv 报 `failed to map segment from shared object`

麒麟 V11 的 KySec 安全框架会拦截未受信的 `.so` 加载（venv 内的 Python 原生扩展即是）。注意 `failed to map segment from shared object` 也可能由 noexec 挂载或普通权限问题引起，请先确认确为 KySec 拦截再授权。

优先按应用提示对具体被拒的库文件逐文件授权（桌面端会保留被拦截的依赖环境，授权后重启应用即可原地复检，无需重新下载依赖）：

```bash
kyexectl -g <应用日志中提示的 .so 路径>          # 查看当前标签
sudo kyexectl -s -o <同一路径>                   # 仅信任该文件
```

依赖较多时可对整个安装目录递归打信任标签：

```bash
sudo kyexectl -s -r /opt/wanwei-shuyi-desktop     # RPM / DEB 安装
# 源码方式:
sudo kyexectl -s -r /home/<user>/wanwei
```

验证：`kyexectl -g <任意一个 .so 路径>` 应显示 `verified`。

### 6.2 检索走了 FTS 而不是原生向量

1. 确认系统向量引擎在运行：`systemctl status kylin-ai-vector-engine`（或查看 `/var/tmp/kylin-ai-vector-engine-*.sock`）。
2. 确认 bridge 已安装：`ls /usr/local/bin/wanwei-kylin-sdk-bridge`。
3. 检索响应的 `retrieval.backend` 字段如实标注了当前通道（`kylin_native` / `fts_fallback`）。

### 6.3 数据与日志位置

数据目录按启动方式不同而不同：

| 启动方式 | 目录 | 内容 |
|---|---|---|
| 桌面应用（RPM/DEB） | `~/.config/wanwei-shuyi-desktop/runtime/` | `memory.db`、`kylin-vector.db`、`backend.log` |
| 源码 / 命令行直接启动后端 | `$WANWEI_DATA_DIR`，未设置时取 `$XDG_DATA_HOME/wanwei-shuyi`，再未设置时取 `~/.local/share/wanwei-shuyi/` | `memory.db`（SQLite WAL）、`kylin-vector.db` |

> 向量库 `kylin-vector.db` 始终与 `memory.db` 同目录（可用 `WANWEI_KYLIN_VECTOR_DB` 单独指定）。
> systemd 用户服务名为 `wanwei-shuyi-desktop.service`，日志用 `journalctl --user -u wanwei-shuyi-desktop`；桌面应用内嵌后端的日志直接看上表的 `backend.log`。

### 6.4 麒麟 V11 上 detached 进程访问回环报 `EPERM`

2026-09-08 麒麟 V11 虚拟机实测观测（issue #206-E，`reports/kylin-vm-evidence-20260908/09-*` / `09b-*`）：用 `nohup` 脱离 SSH 会话启动的 Python 进程访问本机回环端口会报 `EPERM`，**同批 `curl` 不受影响**；在同一前台会话（attached）内运行 curl、venv Python、系统 Python 三个客户端均返回 200，**不复现**（detached 侧复现 3 次）。

这是 V11 的环境行为观测，不是产品缺陷。应对：

- 演示与验证脚本（含 `scripts/demo_governance.py`）请在**前台会话**内运行，不要用 `nohup` 脱离会话；
- 需要常驻的服务由 **systemd 用户单元**拉起（有会话归属），避免纯 `nohup` 后台运行 Python 客户端。

证据与归因详见 [competition/09-vm-evidence.md](competition/09-vm-evidence.md) 第三节。

---

## 七、卸载

```bash
sudo rpm -e wanwei-shuyi-desktop        # RPM
sudo dpkg -r wanwei-shuyi-desktop       # DEB
```

卸载会移除 `/opt/wanwei-shuyi-desktop/`、`/usr/bin/wanwei-shuyi-desktop` 与 `/etc/systemd/user/wanwei-shuyi-desktop.service`，但**保留用户数据目录 `~/.config/wanwei-shuyi-desktop`**（内含记忆数据库），避免误删。如需彻底清除请手动执行：

```bash
rm -rf ~/.config/wanwei-shuyi-desktop
```

---

*本手册对应版本 v1.0.0。变更历史见 [CHANGELOG.md](CHANGELOG.md)。*
