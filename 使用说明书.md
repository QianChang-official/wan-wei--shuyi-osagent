# 宛委·枢忆 使用说明书

面向最终用户与运维的安装、配置与日常使用指南。开发者文档（架构设计、创新点、评测口径）已随 v1.0 发布归档精简；本手册只回答「怎么装、怎么用、怎么排障」。

---

## 一、系统要求

| 项目 | 要求 |
|---|---|
| 操作系统 | 银河麒麟 V11（x86_64）桌面版；其他 Linux 发行版可源码运行 |
| Python | 3.10+（麒麟 V11 自带 3.12，无需另装） |
| Node.js | 22.12+（仅从源码构建桌面端时需要；安装 RPM/DEB 包不需要） |
| 磁盘 | ≥ 2 GB（含向量库与日志） |
| 内存 | ≥ 4 GB |
| 可选 | 麒麟原生 AI 向量引擎（`kylin-ai-vector-engine`，系统预装）——启用后走原生语义检索 |

---

## 二、安装

### 方式 A：RPM 安装包（推荐，麒麟 V11）

```bash
# 联网环境：安装时会自动拉取 Python 依赖
sudo dnf install ./wanwei-shuyi-v1.0.0-1.x86_64.rpm
# 或
sudo rpm -ivh wanwei-shuyi-v1.0.0-1.x86_64.rpm
```

安装内容：
- 桌面应用：`/opt/wanwei-shuyi/`（Electron 主程序，应用菜单可直接启动）
- 后端服务随桌面应用自动拉起（本机回环 127.0.0.1，不对外暴露）

### 方式 B：DEB 安装包（Debian 系）

```bash
sudo dpkg -i wanwei-shuyi-desktop_1.0.0_amd64.deb
```

### 方式 C：源码运行（开发者）

```bash
bash scripts/setup.sh        # 创建 venv 并安装依赖
bash scripts/run_dev.sh      # 启动后端 + 控制台
```

控制台入口：**http://127.0.0.1:8010/console/**

---

## 三、首次启动

1. 启动桌面应用（应用菜单搜索「宛委」），或源码方式运行 `run_dev.sh`。
2. 后端首次启动自动完成数据库初始化（SQLite WAL 模式，无需手工建库）。
3. 进入控制台「万枢工作台」即可对话——未配置模型时走 `local_mock` 通路，零配置可跑通全流程。
4. 接入真实模型：进入「模型接入」视图，从 31 家供应商目录中任选一家，填入 API key 保存。OpenAI 兼容云端供应商（含 DeepSeek）与 AWS Bedrock 为真实调用，其余标注 stub。

**桌面应用启动后无响应？** 见第六节 KySec 排障。

---

## 四、日常使用

### 4.1 记忆指令

在控制台任意对话中说「记住……」，系统经 Policy Gate 校验后写入记忆胶囊（拦截密码 / 密钥 / 投毒内容）。可在「记忆中枢」视图查看与编辑。

### 4.2 记忆治理端点（嵌入式集成）

所有端点默认仅监听本机回环；写操作需带 `X-API-Key` 请求头（回环 GET 免密只读）。

| 域 | 端点 | 说明 |
|---|---|---|
| 写入 | `POST /memory/v2/capsules` | 写入记忆胶囊（过 Policy Gate） |
| 检索 | `GET /memory/v2/search?q=...&top_k=5` | 混合检索（原生向量 / FTS 自动选择） |
| 遗忘 | `POST /memory/forget/confirm` | 删除并五处取证 |
| 删除证明 | `GET /memory/governance/verify-deletion/{id}/certificate` | 导出 PDF 删除证书 |
| 账本 | `GET /memory/ledger/{id}` | 单条记忆的不可变操作历史 |
| 健康度 | `GET /memory/health` | 记忆库健康评分 |

### 4.3 一键证据演示

```bash
python scripts/demo_governance.py --api-key <key>
```

五步走完「写入 → 召回 → 删除 → 五处取证 → PDF 证书」全链路。

### 4.4 常用脚本

| 脚本 | 用途 |
|---|---|
| `scripts/run_dev.sh` | 启动后端 + 控制台 |
| `scripts/smoke.sh` / `smoke.py` | 冒烟自检 |
| `scripts/demo_governance.py` | 五步证据链演示 |
| `scripts/run_meb.py` | MEB 记忆评测（mini/full） |
| `scripts/bench_latency_scale.py` | 检索延迟规模基准（1k/10k/50k） |
| `scripts/bench_tke.py` | TKE 知识演化基准（as-of 回放正确率） |

---

## 五、配置（环境变量）

| 变量 | 默认 | 说明 |
|---|---|---|
| `WANWEI_API_KEY` | 无 | API key；未设置时写操作仅限本机回环且需 key，生产部署必须设置（≥32 字符） |
| `WANWEI_MEMORY_DB` | `data/memory.db` | SQLite 数据库路径 |
| `WANWEI_KYLIN_NATIVE_MODE` | `auto` | `auto` 优先麒麟原生向量检索；`off` 强制 FTS |
| `WANWEI_KYLIN_SDK_PERSISTENT` | `1` | 常驻原生 SDK bridge（模型只加载一次，检索延迟 ~15ms）；`0` 回退每次 spawn 的旧模式（排障用） |
| `WANWEI_KYLIN_SDK_TIMEOUT_SECONDS` | `10` | 原生 SDK 单请求超时 |
| `WANWEI_CORS_ORIGINS` | 无 | 跨源白名单（逗号分隔）；不接受 `*` |
| `WANWEI_OUTCOME_VALIDATION` | off | 偏好执行结果反馈闭环开关 |

---

## 六、排障

### 6.1 桌面应用 / venv 报 `failed to map segment from shared object`

麒麟 V11 的 KySec 安全框架会拦截未受信的 `.so` 加载（venv 内的 Python 原生扩展即是）。对安装目录打一次信任标签即可：

```bash
sudo kyexectl -s -r /opt/wanwei-shuyi     # RPM 安装
# 源码方式:
sudo kyexectl -s -r /home/<user>/wanwei
```

验证：`kyexectl -g <任意一个 .so 路径>` 应显示 `verified`。

### 6.2 检索走了 FTS 而不是原生向量

1. 确认系统向量引擎在运行：`systemctl status kylin-ai-vector-engine`（或查看 `/var/tmp/kylin-ai-vector-engine-*.sock`）。
2. 确认 bridge 已安装：`ls /usr/local/bin/wanwei-kylin-sdk-bridge`。
3. 检索响应的 `retrieval.backend` 字段如实标注了当前通道（`kylin_native` / `fts_fallback`）。

### 6.3 数据与日志位置

| 内容 | 路径 |
|---|---|
| 记忆数据库 | `<安装目录>/data/memory.db`（SQLite WAL） |
| 向量库 | `<安装目录>/data/kylin-vector.db` |
| 后端日志 | systemd 用户服务：`journalctl --user -u wanwei-backend`；桌面应用：`~/.local/share/wanwei/` |

---

## 七、卸载

```bash
sudo rpm -e wanwei-shuyi        # RPM
sudo dpkg -r wanwei-shuyi-desktop   # DEB
```

卸载保留用户数据目录（`~/.local/share/wanwei/`），如需彻底清除请手动删除。

---

*本手册对应版本 v1.0.0。变更历史见 [CHANGELOG.md](CHANGELOG.md)。*
