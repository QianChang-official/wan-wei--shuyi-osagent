# 宛委·万枢 —— 麒麟桌面级 AI 聊天与开发协作平台 · 架构设计

> 文档状态：M1 冻结版（随本平台首批八模块交付同步）
> 读者对象：平台建设者、并行开发代理、评审与后续维护者
> 项目根：`wan-wei--shuyi-osagent/`（FastAPI `backend/` + Vue3 控制台 `frontend/console-vue/` + Electron 桌面端 `desktop/`）
> 参考资料：工作区根 `design.md`（国风双主题设计规范）、`docs-research/kylin-orca-architecture-notes.md`（麒麟规范与 Orca 理念调研）
> 边界声明：本文描述的端点与能力以代码为最终契约；凡标注「stub / simulated / 规划」者均不构成已可用承诺。

---

## 1. 愿景与定位

### 1.1 一句话定位

「宛委·万枢」是面向麒麟 Linux（KylinOS V10/V11，UKUI 桌面）的**桌面级 AI 聊天与开发协作平台**：
以本地 FastAPI 服务为枢、以国风双主题 Vue 控制台为面、以 Electron 原生壳为体，
把「多模型对话、多智能体并行编排、任务空间隔离、知识库、长期记忆、自动化、MCP 工具枢纽」
收进一台信创桌面，做到**运行时核心功能可离线、密钥不出机、改动必经人审**；
桌面端首次启动仍需联网安装 Python 依赖，纯离线环境须预先准备依赖包。

命名取义：宛委（传说中藏书之山）主记忆与知识，万枢（枢机万千）主调度与协作。
桌面壳沿用「枢忆·花朝」之名，记忆底座复用「宛委·枢忆」既有研发成果。

### 1.2 为什么是「桌面级」而非「Web 移植」

本平台不是把 Web 控制台套一层浏览器壳。判定一个桌面应用是否「原生级」，
看的是它是否接管了浏览器给不了的系统能力。万枢的桌面级论据如下：

1. **后端守护**：Electron 主进程直接 spawn 并守护 FastAPI 子进程——随机空闲端口、
   `/health` 探活（60 秒超时）、异常退出弹窗指引日志、退出时 SIGTERM（5 秒不退则 SIGKILL）。
   用户双击图标即得完整服务，无需先装 Python、开终端、跑 uvicorn。
2. **系统托盘**：关闭窗口即最小化到托盘常驻（UKUI 通知区标准行为），托盘菜单提供
   显示主窗口 / 浏览器打开 / 自启动开关 / 测试通知 / 退出，符合桌面应用惯例而非网页惯例。
3. **开机自启动**：走 XDG Autostart（用户级 `~/.config/autostart/`）+ Electron
   `setLoginItemSettings` 双通道，做成**应用内开关**由用户主动开启——避开麒麟 V10
   对非白名单自启动项的拦截策略，不做 postinst 强写。
4. **防睡眠**：`powerSaveBlocker` 两档——`app` 仅阻止系统挂起、`display` 连同屏幕常亮，
   保证长时编排运行（agent 跑任务）期间机器不睡。
5. **局域网**：一键切换后端监听 `127.0.0.1 ↔ 0.0.0.0`（热重启后端完成切换），
   自动优选私有网段 IPv4（10./192.168./172.16-31.）生成手机访问地址，
   配合 `/mobile` 页面与 LAN token 构成「移动伴侣」通道。
6. **浮动窗**：420×640 无边框、置顶、跳过任务栏的浮动工作区小窗，
   加载 `/console/#/mobile?floating=1`，让「随手问一句」不占用整个桌面。

另有桌面级基础件：单实例锁（二次启动仅唤起已有窗口）、窗口尺寸位置记忆、
freedesktop 桌面通知、本地文件打开/保存对话框（8MB 读入上限）、
外部链接一律交系统浏览器、`contextIsolation` + `nodeIntegration: false` 的渲染隔离。

### 1.3 Electron 主进程能力清单

| 能力 | 实现要点 | 状态 |
| --- | --- | --- |
| 后端守护 | spawn uvicorn + `/health` 探活 + 日志落 `runtime/backend.log` + 退出码处理 | 已落实 |
| 随机端口与密钥 | 启动期探测空闲端口；API Key 首启生成，`0600` 仅存 userData | 已落实 |
| 系统托盘 | Tray + 上下文菜单 + 关闭即隐藏 + 单击显隐切换 | 已落实 |
| 开机自启动 | XDG autostart 文件 + `setLoginItemSettings`，应用内开关 | 已落实 |
| 防睡眠 | `powerSaveBlocker`：app / display 两种模式，托盘可切 | 已落实 |
| 局域网模式 | 后端热重启切换 `0.0.0.0`；私有网段 IP 优选；`/mobile` 入口 | 已落实（token 有效期 15 分钟且仅可成功使用一次；关闭或重新开启 LAN 会吊销旧 token） |
| 浮动窗 | 无边框置顶小窗加载移动视图，随时唤起/销毁 | 已落实 |
| 主题跟随 | `nativeTheme` 监听 UKUI 明暗切换，推送渲染进程换「靛夜」主题 | 已落实 |
| 单实例与窗口态 | `requestSingleInstanceLock`；尺寸/位置/最大化持久化 | 已落实 |
| 桌面通知与文件 IPC | `desktop:notify / open-file / save-file / show-item / info / *-autostart` | 已落实 |
| deb 打包上架 | electron-builder + 自包含 Python 运行时 | 规划（M3） |

### 1.4 与既有研发底座的关系

万枢平台**长在**既有「宛委·枢忆 MemoryOps」底座之上，不推倒重来：
记忆运行时（MemoryCapsule 2.0、Policy Gate、审计）、SQLite FTS5 检索、
模型网关的 local_mock 离线通路全部保留并继续可用；
麒麟官方 embedding/vector SDK 的原生向量检索当前尚未接入知识库，后续路线图评估。
万枢在其上新增的是**协作层**——平台 API 聚合包
`backend/app/platform_api/`、控制台 `views/platform/` 视图族、桌面壳六项系统能力。

---

## 2. Orca 架构理念映射

Orca（stablyai/orca）证明了「worktree-native + 多智能体并行 + 人在环审查」这一
范式在桌面端的成立性。万枢取其神而不抄其形（Orca 是编辑器中心，万枢是控制台中心）：

| Orca 理念 | 万枢映射 | 落地说明 |
| --- | --- | --- |
| 并行 git worktree | **tree / main / perch 三态空间**（spaces 模块） | alpha 期为语义化分支角色标注（main=主干、tree=集成暂存、perch=实验栖枝），真实 git worktree 绑定列入 M2 |
| 多智能体并行 | **编排运行 + 思考深度六档 + 工作档位三档**（agents 模块） | 一次编排 = 一个 run，可挂单 agent 或团队；深度决定思考预算，档位决定触达面；human_review 档关键步骤挂起等待 `/approve` |
| 移动伴侣 | **`/mobile` 页面 + LAN 模式 + LAN token** | alpha 期手机端主要展示状态与 token 配对校验；写操作受 API Key 中间件保护，手机浏览器无法直接执行（需后续 LAN token 换受限会话） |
| Orca CLI | **wanwei CLI 使用指南** | 当前为 `/system/cli/guide` 返回的静态命令说明；真实 CLI 工具列入 M2 |
| Annotate AI Diff | **人工审查档位（human_review）** | 当前提供采纳/驳回；diff 行级批注与「追问」三态列入 M2 |
| Design Mode | **后续路线** | 元素拾取（截图+HTML/CSS 进 prompt）列入 M3 之后，与麒麟截图/录屏权限模型一并评估 |
| Agent 中立（CLI 适配器） | **providers 的 OpenAI 兼容聚合 + mcp_hub 工具接入** | 模型走 API 聚合层，工具走 MCP；两层解耦，任意一家失效不拖垮平台 |
| SSH/远程执行分层 | 路线图候选 | 单节点 alpha 不做；沙盒远置、算力外移留待 M3 之后评估 |

三条主动取舍：

1. **物理隔离优于流程约束**：并行任务必须各占独立分支与目录，不依赖 agent 自觉。
2. **人是瓶颈**：平台内置 diff 聚合与批量归档，默认并行度保守；审查界面是一等功能。
3. **离线优先**：麒麟/信创场景默认零遥测、local_mock 可全链路演示，
   所有真实外部调用「配置就绪才启用」。

---

## 3. 麒麟标准符合性清单

状态口径：**已落实** = 代码中存在且可用；**部分** = 主干就绪、尚有收尾项；**规划** = 仅设计。

| # | 条目 | 状态 | 落实位置 / 备注 |
| --- | --- | --- | --- |
| 1 | XDG 目录规范 | 已落实 | 配置/数据/缓存全走 Electron `userData`（`~/.config/wanwei-shuyi-desktop/`）与 XDG 语义；平台数据经 `WANWEI_PLATFORM_DIR` 可重定向，默认项目内 `data/platform/`（开发态），打包后指向用户数据区 |
| 2 | XDG Autostart 自启动 | 已落实 | 用户级 `~/.config/autostart/wanwei-shuyi-desktop.desktop`，应用内开关；不写系统级、不在 postinst 强写（规避 Kysec 白名单拦截） |
| 3 | UKUI 主题跟随 | 已落实 | `nativeTheme` 事件 + 前端 `prefers-color-scheme` 双通道；day/night 双主题全部走 CSS 变量，禁写死色值 |
| 4 | 托盘与通知 | 已落实 | Tray 符合 StatusNotifier 惯例；通知走 freedesktop（libnotify）；无任何广告式弹窗（Kysec 红线自查通过） |
| 5 | 单实例 / 窗口状态 | 已落实 | 单实例锁 + 窗口几何持久化，UKUI 桌面体验一致 |
| 6 | 防睡眠与电源 | 已落实 | `powerSaveBlocker` 双模式；不请求任何特权 |
| 7 | 沙盒与权限模型 | 部分 | 设计采用用户态隔离（bubblewrap/namespace 方向），不依赖 systemd 服务与内核模块；agent 执行面由三档 gear 门禁约束；device 档与 sandbox 同为可执行档位，整机级危险操作由具体模块显式校验 |
| 8 | 密钥与凭据 | 部分 | 平台密钥 Fernet 加密落盘（见 §6.2）；桌面 API Key `0600`；系统钥匙串（libsecret）托管列为 M2 项 |
| 9 | deb 打包规划 | 规划 | M3：electron-builder 出 deb（amd64/arm64），mips64el/loongarch64 走离线 Electron + electron-packager；Python 运行时 PyInstaller 自包含进 `/opt/apps/<appid>/`，不依赖系统 Python |
| 10 | desktop 入口与图标 | 规划 | 随 deb 交付 `/usr/share/applications/*.desktop`（含 `Name[zh_CN]`）+ hicolor 多尺寸图标（≥512×512） |
| 11 | Kysec 注意事项 | 部分 | 设计期已规避：无捆绑、无广告弹窗、无非常驻特权、自启动用户态开关；上架前需过「包信息→格式→安全→验签」检测链并备签名（ukey/GPG） |
| 12 | KYSDK / 统一认证 / PAM | 规划 | 深度 UKUI 集成（系统搜索、统一配置、生物识别）评估为 P2，Electron 侧需 native 桥，不阻塞主线 |

---

## 4. 系统架构

### 4.1 总体架构图（ASCII）

```
┌─────────────────────────── 麒麟 Linux 桌面（UKUI） ───────────────────────────┐
│ ┌─────────────────────── Electron 主进程 main.js ───────────────────────┐   │
│ │ 后端守护 · 托盘 · 自启动 · 防睡眠 · LAN 切换 · 浮动窗 · 单实例 · 通知    │   │
│ └───────┬───────────────────────────────────────────────┬──────────────┘   │
│         │ spawn / 守护 / 热重启                          │ preload 桥        │
│         ▼                                               ▼                  │
│ ┌───────────────────┐   同源 REST + X-API-Key   ┌───────────────────────┐  │
│ │  FastAPI 后端      │ ◀──────────────────────▶ │ Vue3 渲染进程（控制台） │  │
│ │  (uvicorn 子进程)  │   /console/ 静态托管      │ hash 路由 · 双主题     │  │
│ └─────────┬─────────┘                          └───────────────────────┘  │
└───────────┼────────────────────────────────────────────────────────────────┘
            │ 浏览器 / 手机(LAN) 亦可直连同一服务（wanwei CLI 为规划工具，见 §5.7）
┌───────────▼────────────────────────────────────────────────────────────────┐
│ app.main ──/platform──▶ platform_api 包自动发现聚合（模块故障隔离，单点跳过）│
│   providers · agents · spaces · automation · knowledge · memory_center     │
│   system_svc · mcp_hub                                                     │
└───┬──────────────┬───────────────┬────────────────┬────────────────────────┘
    ▼              ▼               ▼                ▼
┌─────────┐  ┌───────────┐  ┌─────────────┐  ┌──────────────────────────┐
│JsonStore│  │SQLite FTS5│  │ 模型网关     │  │ 既有底座：记忆运行时/梦境 │
│platform_│  │ memory.db │  │ OpenAI 兼容 │  │ 审计/工作流/Kylin SDK   │
│*.json   │  │（记忆检索）│  │ 聚合+mock   │  │ （原生向量检索：待评估） │
└─────────┘  └───────────┘  └──────┬──────┘  └──────────────────────────┘
                                   ▼
                     31 家模型目录（配置就绪才真实调用）
```

要点：后端模块**零侵入**扩展——新建 `backend/app/platform_api/<模块>.py` 并暴露顶层
`router = APIRouter()` 即被自动挂到 `/platform`；单模块导入失败仅告警跳过，不拖垮整体。
持久化统一 `JsonStore`（整文件读写 + 线程锁 + tmp 替换原子写），
共享枚举集中在 `platform_api/deps.py`，杜绝多头定义漂移。

### 4.2 前端视图分层

```
frontend/console-vue/src/
├─ views/platform/            域视图（hash 路由，base /console/）
│   ├─ WorkbenchView          工作台（总览入口）
│   ├─ ProvidersView          模型接入      ├─ AgentsView      智能体编排
│   ├─ SpacesView             三态空间      ├─ AutomationView  自动化
│   ├─ KnowledgeView          知识库        ├─ MemoryCenterView 记忆中心
│   ├─ SessionsView           会话          ├─ MobileView      移动伴侣/浮动窗
│   └─ SettingsView / HelpView 设置与帮助
├─ api/platform.ts            apiGet/apiPost/apiPut/apiDel（base 已含 /platform）
├─ components/gf/             国风共享组件：GfCard/GfTag/GfButton/GfStat/GfEmpty/
│                             PageHero/InkDivider/ThemeToggle/PetalFall 等
└─ tokens.css                 宣纸·白昼 / 靛夜·灯影 双主题 CSS 变量（禁写死色值）
```

前端纪律：视图对缺字段容错（可选链 + 默认值 + 离线兜底示例数据）；
样式只用主题变量与 gf 组件；图标禁用 emoji，文字与 CSS 图形代之。

### 4.3 关键数据流：一次编排运行

```
用户在 AgentsView 发起编排
  → POST /platform/agents/run（含 depth 深度 + gear 档位 + task/goal）
  → agents 校验 gear 与 depth 枚举，创建 run 记录（JsonStore）
  → 后台 asyncio task 逐步推进 plan → act → review → reflect
  → 模型调用优先走 model_gateway（配置就绪→gateway，否则→mock）；结果以 engine 字段诚实标注
  → gear=human_review：act/review 步骤进入 `awaiting_review`，须 POST /runs/{rid}/approve 后才继续
  → run 最终状态落 JsonStore；前端通过 GET /runs/{rid} 轮询，无服务端推送
  → 记忆中心 `/remember` 等写操作由用户显式触发；梦境整理当前为手动触发（`/dreams/archive-now`）
（独立分支/目录、diff 行级批注、夜间自动调度、全平台统一审计留痕列入 M2）
```

---

## 5. 模块目录

> 端点均以 `/platform` 为前缀；下表以 **当前代码实际注册路由** 为事实源，
> M1 文档中的旧路径已按代码修正。路由顺序仍遵循：固定路径先于参数路径注册，
> 避免参数路由吞掉固定段。

### 5.1 providers —— 模型接入（31 家）

职责：统一维护 31 家模型厂商目录（catalog）与用户接入配置（configs），
密钥 Fernet 加密后落盘；接口只写不读（回显掩码/尾 4 位）。
本地类 provider（lm_studio、ollama_cloud）配置就绪时真实探测 base_url，
其余云端/聚合/OAuth 类在密钥未配置时明确失败并说明原因，
密钥已配置但真实连通性测试尚未接入时返回 `mode:'stub'` 诚实标识。
辅助模型 `/providers/aux` 提供对话标题生成、摘要等辅助任务的统一路由出口。
OAuth 设备授权流程当前为模拟 stub，真实流程配置就绪后启用。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/platform/providers/catalog` | 31 家内置目录（只读） |
| GET | `/platform/providers/configs` | 全部 31 家配置视图（未配置按目录默认占位） |
| PUT | `/platform/providers/configs/{pid}` | 新建/更新配置（api_key Fernet 加密落盘） |
| DELETE | `/platform/providers/configs/{pid}` | 删除配置 |
| POST | `/platform/providers/test` | 连通性测试（本地真实探测 / 其余 stub） |
| GET/PUT | `/platform/providers/aux` | 辅助模型配置读取/更新 |
| POST | `/platform/providers/auth/{pid}/begin` | OAuth 设备授权开始（stub） |
| POST | `/platform/providers/auth/{pid}/poll` | OAuth 设备授权轮询（stub） |

### 5.2 agents —— 智能体编排

职责：智能体 CRUD、团队 CRUD、编排运行 run、浮动会话与上下文估算。
智能体包含角色、人格、思考深度、工作档位、权限面、provider/model/goal。
团队支持 sequential / parallel / review_loop 三种编排。
run 在后台 asyncio task 中推进 plan/act/review/reflect，
`gear=human_review` 时 `act`/`review` 步骤进入 `awaiting_review` 等待人工放行；
结果优先尝试 `model_gateway`（配置就绪才真实调用），失败回退模拟并以
`engine:'mock'|'gateway'` 诚实标注。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/platform/agents` | 智能体列表 / 创建 |
| GET/PUT/DELETE | `/platform/agents/{aid}` | 单智能体详情 / 更新 / 删除 |
| GET/POST | `/platform/agents/teams` | 团队列表 / 创建 |
| GET/PUT/DELETE | `/platform/agents/teams/{tid}` | 团队详情 / 更新 / 删除 |
| POST | `/platform/agents/run` | 发起一次编排运行 |
| GET | `/platform/agents/runs` | 运行列表 |
| GET | `/platform/agents/runs/{rid}` | 运行详情 |
| POST | `/platform/agents/runs/{rid}/approve` | 审查档位下人工放行 / 驳回（`approved:false` 即驳回并终止运行；「追问」与 diff 行级批注列入 M2） |
| POST | `/platform/agents/runs/{rid}/cancel` | 取消运行 |
| POST | `/platform/agents/chat` | 与智能体/团队对话 |
| POST | `/platform/agents/subagent` | 创建浮动子代理 |
| GET | `/platform/agents/workspace/floating` | 浮动会话列表 |
| GET | `/platform/agents/context-size` | 按 agent_id 估算上下文 token |

### 5.3 spaces —— 项目空间与 tree/main/perch 三态

职责：承载「并行 worktree」理念的项目实体。以 **项目（project）** 为管理单元，
`kind` 区分 `project_space`（项目空间）与 `task_space`（任务空间）。
每个项目配置 `root_path` 指向真实 git 仓库时，三态视图读取真实分支；
否则返回语义化模拟数据并标注 `source:'simulated'`。
账号绑定（github / linear）token 用 Fernet 加密落盘，任何接口不回显明文。
提交模板与提交执行默认 `dry_run=True`；真实提交仅允许在 `sandbox`/`device` 档位，
`human_review` 档位只允许预览。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/platform/spaces/projects` | 项目列表（可按 kind 过滤）/ 创建 |
| GET/PUT/DELETE | `/platform/spaces/projects/{pid}` | 项目详情 / 更新 / 删除 |
| GET | `/platform/spaces/integrations` | 账号绑定列表 |
| POST | `/platform/spaces/integrations/{kind}/bind` | 绑定 github / linear 账号 |
| POST | `/platform/spaces/integrations/{kind}/unbind` | 解绑 |
| POST | `/platform/spaces/integrations/{kind}/test` | 连通性测试（github 真实 / linear stub） |
| GET | `/platform/spaces/{pid}/tree` | 三态分支视图（git 真实或 simulated） |
| GET/PUT | `/platform/spaces/{pid}/commit-template` | 提交模板读取 / 更新 |
| POST | `/platform/spaces/{pid}/commit` | 提交预览或执行 |

### 5.4 automation —— 自动化工作流

职责：AI 可编辑工作流与自动化运行。工作流 CRUD 持久化于 `JsonStore('flows')`；
自然语言指令经规则式中文解析器转成流程定义 diff，并以 `engine:'mock'` 诚实标注；
运行模拟在 asyncio 后台逐步执行，shell/http/agent/memory 步骤不真实执行，
仅返回 `would_run` 说明与计划；cron 五段式仅做标准库粗算下次触发时间，
复杂组合标注 `approximate=true`。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/platform/automation/flows` | 工作流列表 / 创建 |
| POST | `/platform/automation/flows/ai-edit` | 自然语言指令 → 流程 diff（mock 引擎） |
| GET | `/platform/automation/flows/schedule/overview` | 定时触发概览 |
| GET/PUT/DELETE | `/platform/automation/flows/{fid}` | 工作流详情 / 更新 / 删除 |
| POST | `/platform/automation/flows/{fid}/ai-apply` | 应用 AI 生成的流程 diff |
| POST | `/platform/automation/flows/{fid}/run` | 触发一次运行（202 异步） |
| GET | `/platform/automation/runs` | 运行历史列表 |
| GET | `/platform/automation/runs/{rid}` | 单次运行详情 |

### 5.5 knowledge —— 知识库

职责：文档的收录、检索与出处管理。当前实现为 **SQLite FTS5 全文检索**
（FTS5 不可用时回退 LIKE），响应中通过 `engine` 字段诚实标注实际通道；
**无外部向量检索后端**。CJK 中文通过入库前逐字插空格适配 unicode61 分词器，
FTS 未命中时自动 LIKE 兜底。批量导入提供 `import` 端点，统计端点返回
来源/标签/近 7 日分布。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/platform/knowledge/docs` | 文档分页列表 / 创建 |
| GET/PUT/DELETE | `/platform/knowledge/docs/{did}` | 文档详情 / 更新 / 删除 |
| GET | `/platform/knowledge/search` | 全文检索（`engine` 标注 fts5/like/latest） |
| POST | `/platform/knowledge/import` | 批量导入（`imported`/`skipped`） |
| GET | `/platform/knowledge/stats` | 统计（total/by_source/by_tag_top10/recent7d） |

### 5.6 memory_center —— 记忆中心

职责：面向用户的长期记忆门面。持久化走平台共享 `JsonStore`
（`memory_instructions` / `phrases` / `sessions` / `dream_archive` 四个命名空间），
**未**直接复用 MemoryCapsule 2.0 存储；写入侧复用既有记忆运行时的 Policy Gate
（`evaluate_policy`）——记忆指令（「记住」）、`/remember`、常用语三个写入入口统一校验，
触发 `reject`/`quarantine` 时返回 422 并落审计。
梦境整理当前 **无后台调度任务**，`/dreams/schedule` 如实返回
`{"enabled":false,"mode":"manual"}`，需手动调用 `/dreams/archive-now` 触发整理；
无真实会话数据时使用示例会话并标注 `source:'sample'`。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/PUT | `/platform/memory/instructions` | 记忆指令列表 / 全量替换 |
| GET | `/platform/memory/instructions/prompt` | 拼成可注入系统提示的文本块 |
| DELETE | `/platform/memory/instructions/{index}` | 删除单条指令 |
| POST | `/platform/memory/remember` | 「记住」快捷追加入口 |
| GET | `/platform/memory/dreams` | 梦境归档时间线 |
| GET | `/platform/memory/dreams/schedule` | 调度状态（当前手动模式） |
| POST | `/platform/memory/dreams/archive-now` | 手动触发梦境整理 |
| GET/POST | `/platform/memory/phrases` | 常用语列表 / 创建 |
| DELETE | `/platform/memory/phrases/{pid}` | 删除常用语 |
| POST | `/platform/memory/phrases/{pid}/use` | 记录常用语使用 |
| GET | `/platform/memory/sessions` | 会话列表（标注数据源 `JsonStore('sessions')`） |
| POST | `/platform/memory/sessions/{sid}/archive` | 归档会话 |
| POST | `/platform/memory/sessions/{sid}/unarchive` | 恢复归档 |
| PUT | `/platform/memory/sessions/{sid}` | 更新会话字段 |

### 5.7 system_svc —— 系统服务

职责：平台级运行信息与桌面能力的服务端出口。健康检查、防睡眠状态镜像、
通用设置（主题/语言/背景/自启/花瓣，其中 `background_opacity` 固定只读）、
语音输入存档（转写 stub）、防追踪浏览器规则与启动计划、模拟器镜像下载
（后台守护线程模拟推进，不真实拉取大文件）、局域网手机控制 token 与 URL、
沙盒命令执行（白名单 + cwd 监禁 + 5 秒超时 + 4KB 截断）。
所有真实外部副作用遵循「配置就绪才启用，否则明确标注 stub/simulated」。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/platform/system/health` | 聚合健康（当前返回服务可达状态） |
| GET/PUT | `/platform/system/power` | 防睡眠状态查询 / 设置 |
| GET/PUT | `/platform/system/settings` | 通用设置读取 / 更新 |
| POST/GET | `/platform/system/voice` | 语音存档 / 历史列表 |
| GET/POST | `/platform/system/browser/rules` | 拦截规则列表 / 新增 |
| PUT | `/platform/system/browser/rules/{rid}` | 规则更新 |
| POST | `/platform/system/browser/launch` | 启动计划（桌面端执行） |
| GET | `/platform/system/emulator/downloads` | 模拟器镜像下载项列表 |
| POST | `/platform/system/emulator/downloads/{did}/start` | 开始模拟下载 |
| POST | `/platform/system/emulator/downloads/{did}/cancel` | 取消下载 |
| GET | `/platform/system/lan/status` | LAN 状态 |
| POST | `/platform/system/lan/enable` · `/disable` | 开启 / 关闭局域网控制 |
| POST | `/platform/system/lan/verify` | 校验配对 token |
| GET | `/platform/system/sandbox/whitelist` | 沙盒命令白名单与监禁目录 |
| POST | `/platform/system/sandbox/exec` | 沙盒内执行白名单命令 |
| GET | `/platform/system/cli/guide` | wanwei CLI 使用指南（静态命令说明；CLI 本体未实现，列入 M2） |

### 5.8 mcp_hub —— MCP 工具枢纽

职责：MCP（Model Context Protocol）服务器注册表与工具调用代理。
支持 stdio/sse/streamable_http 三种传输定义注册；stdio 传输仅在
`enabled=True`、服务端显式开启 device 档且 command 命中
`WANWEI_MCP_STDIO_COMMANDS` 白名单时真实启动子进程并通过 JSON-RPC 探测工具清单；
子进程使用最小环境，只接收该服务器显式配置的 env，不继承 `WANWEI_*` 服务秘密。
否则返回 `stub` 调用计划与状态说明。预置 3 条示例服务器默认不启用且
`command` 为空，启用前需用户自行填入可执行命令。平台自身能力经 MCP 反向暴露
规划为 M2 主线，当前版本未实现。

关键端点：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/platform/mcp/servers` | 服务器注册表列表 / 新增 |
| GET/PUT/DELETE | `/platform/mcp/servers/{sid}` | 详情 / 更新 / 注销 |
| GET | `/platform/mcp/servers/{sid}/tools` | 工具清单（未连通→空表+说明） |
| POST | `/platform/mcp/servers/{sid}/call` | 单服务器工具调用代理 |
| GET | `/platform/mcp/overview` | 服务器/启用数/工具数/最近调用汇总 |

## 6. 安全与边界

### 6.1 单节点 alpha 定位

本平台当前为**可运行单节点 alpha**：默认绑定 `127.0.0.1`、单进程 SQLite/JSON 持久化、
不提供多副本高可用与生产 SLA；LAN 模式是显式开关而非默认，且须凭 token 访问。
任何对外暴露面扩大（公网、多用户）都属路线图事项，需重新评审安全模型。

### 6.2 密钥管理（Fernet）

- providers 的各家 API 密钥：**Fernet 对称加密**后随 JsonStore 落盘，
  接口只写不读（回显掩码），日志与事件流一律脱敏。
- 主密钥：由桌面端首启生成，`0600` 存于用户数据区，与数据文件分离；
  环境变量可覆盖以支持无桌面部署。`cryptography` 已在后端依赖内，无新增依赖。
- 桌面通道 API Key：随机生成、`0600`，经 preload 注入渲染进程，主进程兜底补请求头；
  LAN token 与主密钥分离；有效期 15 分钟，首次验证成功即作废；关闭或重新开启 LAN 也会吊销旧 token。

### 6.3 沙盒白名单与 gear 门禁

- 三档 gear 是**硬门禁**：`human_review` 档 agent 只产出建议与 diff，任何真实落盘/提交/执行须经人工确认；`sandbox` 档仅放行白名单目录与命令，越界拒绝并审计；`device` 档在真实落盘/提交场景与 `sandbox` 同列为可执行档位（如 spaces 真实提交），但整机级危险操作仍由具体模块显式校验，不表示无限权限。
- 沙盒实现坚持用户态隔离路线（bubblewrap/namespace 方向），不依赖 systemd 服务、
  内核模块或任何 Kysec 白名单外特权，提权操作走系统授权弹窗（policykit）而非自造密码框。
- 审计：记忆运行时、工作流、沙盒执行、MCP 调用等关键写操作已接入 `audit.service` 留痕；`platform_api` 其余模块当前主要依赖 `JsonStore` 持久化，写操作尚未统一接入 audit 管线，后续路线图补齐。本版本不宣称“全程”留痕。

### 6.4 stub / simulated 诚实清单

| 能力 | 当前口径 | 何时转真 |
| --- | --- | --- |
| 真实模型 API 调用 | 配置就绪（密钥+端点+测试通过）才启用，否则明确 stub | 用户完成配置即转真 |
| Anthropic / Gemini 原生协议 | catalog 配置 stub，不宣称已接入生产调用 | M2 原生接入 |
| MCP 真实连接 | stdio 传输仅在 `enabled`、服务端 device 授权且 `command` 命中部署白名单时真实启动子进程做 JSON-RPC 探测与调用（状态 `unknown/connected/error`），未就绪返回 `stub` 计划；sse / streamable_http 真实连接未实现 | M2 补齐 sse / streamable_http |
| git worktree 真实绑定 / push / 开 PR | alpha 为 main/tree/perch 语义化分支角色标注（`root_path` 指向真实 git 仓库时读真实分支，否则标注 `source:'simulated'`）；push/PR 未实现 | M2 |
| OAuth 第三方登录 | 模拟设备授权端点已实现（`auth/{pid}/begin`/`poll`，stub）；真实授权流程配置就绪后启用 | M2/M3 评估 |
| 知识库检索 | 当前仅 SQLite FTS5（FTS5 不可用时回退 LIKE），无外部向量检索后端 | 路线图评估是否引入原生向量 |
| device 整台设备档 | 与 sandbox 同为可执行档位；整机级危险操作由具体模块显式校验 | 授权模型评审后补全门禁矩阵 |
| 遥测 | **零遥测**，信创默认离线 | 不计划 |

---

## 7. 路线图

| 里程碑 | 范围 | 验收口径 |
| --- | --- | --- |
| **M1（本批，已完成口径）** | platform_api 八模块骨架与契约端点；桌面六项系统能力；三态空间分支角色标注（main/tree/perch）；JsonStore 持久化；gf 组件与双主题全部视图；离线 stub 全链路可走通 | `import app.main` 通过；前端 `npm run build` 通过；桌面 `node --check` 通过；全模块在无网环境以 stub 可演示完整动线 |
| **M2** | 真实 MCP 连接（stdio/SSE）；真实模型接入（OpenAI 兼容 smoke + Anthropic/Gemini 原生）；LAN token 配对强化与吊销；spaces 绑定真实 `git worktree`；沙盒 bubblewrap 落地；libsecret 钥匙串托管 | 真实密钥下端到端跑通一次「编排→审查→合入」；MCP 至少接通一个真实服务器 |
| **M3** | deb 打包（electron-builder + PyInstaller 自包含，装 `/opt/apps/`）；amd64/arm64 CI 与 mips64el/loongarch64 离线 Electron 方案；ukey/GPG 签名；麒麟商店上架材料；KYSDK 集成评估 | 麒麟 V10/V11 真机安装-启动-自启动-主题跟随-卸载全流程通过；Kysec 检测链过审 |

M3 之后候选：Design Mode 元素拾取、SSH/远程算力分层、多用户与协作后台、
统一认证/PAM 集成——均以麒麟权限模型评审为前置。

---

## 附 A. 名词速查

| 名词 | 含义 |
| --- | --- |
| 万枢 | 本平台总名；亦指平台 API 聚合层 |
| 三态空间 | main（主干 受保护）/ tree（集成树 AI合并暂存）/ perch（栖枝 实验/草稿） |
| 深度六档 | low/medium/high/xhigh/max/ultracode（浅思/常思/深思/极思/穷思/超码） |
| 档位三档 / gear | human_review/sandbox/device（人工审查/沙盒工作/整台设备） |
| 记住指令 | 用户口述「记住……」生成的记忆条目，单条 ≤200 行 |
| 梦境 | 记忆整理任务（当前手动触发 `/dreams/archive-now`，无后台调度）：汇总近期会话标题与轮次生成一条摘要归档 |
| LAN 模式 | 后端监听 `0.0.0.0` + token 访问的局域网移动伴侣通道 |
| JsonStore | `platform_api/store.py` 提供的 JSON 落盘持久化（get/set/all/update） |
| stub / simulated | 配置未就绪时的诚实降级应答标识，不冒充真实调用 |


---

## 附 B. 许可证

本项目采用麒麟/openKylin 生态的国产开源许可证——木兰宽松许可证第2版
（Mulan Permissive Software License, Version 2，Mulan PSL v2），官方中文全文
见仓库根目录 `LICENSE`，官方发布地址 http://license.coscl.org.cn/MulanPSL2 。
该许可证中英文版本具有同等法律效力，如有冲突以中文版为准。第三方依赖仍
遵循其各自许可证（见各依赖元数据），与本项目的许可选择相互独立。
